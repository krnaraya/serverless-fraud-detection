package org.redhat;

import io.quarkus.test.junit.NativeImageTest;

@NativeImageTest
public class NativeDecisionResourceIT extends DecisionResourceTest {

    // Execute the same tests but in native mode.
}